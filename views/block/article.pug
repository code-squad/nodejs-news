extends ../index

block article
  - const joinTitleWithHtml = `<h1>${article.title}</h1>\n${article.rawHtml}`
  .container
    article.markdown-body.
      !{joinTitleWithHtml}
    script(src='/js/vendor/prism.js')
